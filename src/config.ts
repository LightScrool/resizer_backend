import path from 'path';

export const ROOT_FOLDER = path.join(process.cwd(), 'dist');

export const ALLOWED_FILES_EXTENSIONS = ['jpg', 'jpeg', 'png', 'webp'];

export const ORIGINAL_PRESET_ALIAS = 'original';
